---
const metrics = [
  { value: '99.9%', label: 'Data Anonymization Accuracy' },
  { value: '80%', label: 'Faster Development Cycles' }, 
  { value: '100%', label: 'Compliance with Data Regulations' }, 
];
---

<section class="bg-gray-100 py-16">
  <div class="container mx-auto px-4">
    <h2 class="text-3xl font-serif font-bold text-gray-800 text-center mb-12">
      Achieve Measurable Results with Null-ID
    </h2>
    <div class="flex flex-col md:flex-row justify-between items-center md:items-start space-y-12 md:space-y-0">
      {metrics.map((metric) => (
        <div class="text-center md:w-1/3"> 
          <div class="text-5xl font-bold text-blue-700 mb-4">
            {metric.value}
          </div>
          <p class="mt-4 text-lg font-semibold text-gray-700 max-w-[200px] mx-auto">
            {metric.label}
          </p>
        </div>
      ))}
    </div>
  </div>
</section>